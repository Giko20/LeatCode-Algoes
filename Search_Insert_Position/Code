# code
'''python'''

import numpy as np

class Solution(object):
    def searchInsert(self, nums, target):
        self.nums=nums
        self.target=target
        np_array=np.array(self.nums)
        x = np.searchsorted(np_array, self.target)
        return x
